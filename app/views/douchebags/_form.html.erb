<%= simple_form_for(@douchebag) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :how_big %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
